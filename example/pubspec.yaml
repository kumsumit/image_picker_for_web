name: image_picker_for_web_integration_tests
publish_to: none

environment:
  sdk: ^3.3.1
  flutter: ">=3.19.3"

dependencies:
  flutter:
    sdk: flutter
  image_picker_for_web:
    path: ../
  image_picker_platform_interface:
    path: ../
  web: ^0.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
